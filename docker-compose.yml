version: '3.9'
services:
  mongo:
    image: mongo:7
    container_name: skillforge-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: skillforge
    volumes:
      - mongo-data:/data/db
    restart: unless-stopped

volumes:
  mongo-data:
    driver: local


